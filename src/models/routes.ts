export enum PublicRoutes {
  LOGIN = "/login",
}

export enum PrivateRoutes {
  PRIVATE = "/private",
  HOME = "home",
  DASHBOARD = "dashboard",
  PROBLEMS = "problems",
  PROBLEM_ALL = "all",
  PROBLEM_VIEW = "view",
  PROBLEM_EDIT = "edit",
  PROBLEM_FORM = "new",
}
